import { LoginScreen } from "@/components/LoginScreen";

// Kept so older /login/ links keep working; the sign-in lives at the site root.
export default function LoginPage() {
  return <LoginScreen />;
}
